module.exports = function(sequelize, dataTypes){
    let alias = "Capacity";
    let cols = {

    }
    let config = {

    }
    const Capacity = sequelize.define(alias, cols, config)
    return Capacity
}